version: "3.8"
# Alternative: uses the official Node runtime and mounts this ready-to-run application.
services:
  jinhua:
    image: public.ecr.aws/docker/library/node:24-alpine@sha256:333f6b3eca25980d5682c26207665b93c9417786b21760b2764d5821d9704c8a
    container_name: jinhua-friends
    restart: unless-stopped
    working_dir: /app
    command: ["node", "server.cjs"]
    ports: ["${GAME_PORT:-18780}:8080"]
    environment:
      PORT: "8080"
      DATA_DIR: /data
      ROOM_PASSWORD: "${ROOM_PASSWORD:?Set ROOM_PASSWORD in .env first}"
      COOKIE_SECURE: "false"
      TZ: Asia/Shanghai
    volumes:
      - ./:/app:ro
      - ./data:/data
    read_only: true
    tmpfs: ["/tmp:size=16m"]
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    mem_limit: 256m
    cpus: 1.0
    pids_limit: 64
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"
