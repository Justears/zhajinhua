# 后周炸炸炸 v1.2.1 验证记录

审查日期：2026-09-20。已核对 GitHub main 为 f208e370ff37c67d17684d5ed4ad437943d53f4d，并逐文件比较本地 v1.2.0 基线，24 个文件均与远端一致。

## 已执行

- Node.js v24.17.0 执行 70 项测试全部通过：37 项规则引擎测试、8 项 HTTP 联机测试、10 项安全测试、15 项前端状态测试。其中包含 8 人 500 局随机压力测试，检查卡牌唯一、积分守恒和无死锁。
- 安全回归：修改密码撤销旧会话、新密码恢复旧座位、旧版签名升级、实时连接到期、按牌桌计算房主离线、HTTPS 来源校验、静态文件路径隔离及监听参数验证。
- 新增 13 项回归检查，覆盖迟到登录/聊天响应、断网保留房间地址、重试回原桌、聊天草稿隔离、加载期间按钮保护和慢请求到期。先复现旧版失败，再验证修复结果。
- 过期请求通过真实 HTTP 分段上传复现：请求开始后推进测试时钟，再传输剩余正文；确认返回 401，房间数量和存档内容未改变。
- 前端采用 Node VM 与模拟 DOM 验证，不等同于真实浏览器渲染或手机触摸测试。服务端和前端 JavaScript 语法检查通过。
- 标准 node --test 在此环境无法创建子进程（EPERM），改用 Node 官方运行器的同进程模式：`node --test --test-isolation=none test/*.test.cjs`。仓库的 npm test 仍可在正常环境运行相同测试文件。

## 离线包制作方式

GitHub 正式发布使用 .github/workflows/release.yml：从固定基础镜像构建 Docker 映像，在映像内执行测试，完成受限容器启动、健康检查和登录验证后打包。附件上传后再次核对大小和 SHA256，再公开 Release。每个正式包中的 发布验证.md 记录提交和 Actions 链接；以对应运行结果为准。

校验文件在构建时生成，不再把某一次本地构建的镜像校验值固定在源码仓库中。正式包可能与下面描述的本地预备包校验值不同，下载后请使用同一 Release 提供的校验文件。

保留 v1.1.1 中已核验的官方 Node AMD64 基础镜像层，替换应用层并更新镜像配置为 jinhua-friends:1.2.1。基础镜像版本未在本轮升级或进行漏洞扫描。制作脚本校验旧包 SHA256、TAR 头、全部文件层摘要和配置摘要，并逐字节比较新应用层与本版源码。ZIP 压缩数据逐文件解压比较；包内仅含空 data/.keep，不含实际密码、配置密钥或房间存档。

镜像 SHA256 见 SHA256SUMS.txt；ZIP 校验文件随本地安装包提供。本地打包不代表已发布到 GitHub。

## 未执行

- 本轮浏览器控制工具不可用，未重新执行浏览器或真实手机测试。上轮 v1.2.0 的手机尺寸验证不是本轮测试结果。
- 未在此电脑运行 Docker 引擎或执行 docker load / 容器实际启动；尚未在用户 DS923+ 上安装测试。
- 未验证用户的 DSM 反向代理、IPv6 入站规则或后端端口外网可达性，也未修改 NAS 配置。
- Compose 的本机绑定、cap_drop 和资源限制需在目标 Container Manager 上验证。绑定 127.0.0.1 不能替代对实际 Docker 版本和防火墙的核对，见 SECURITY.md。
- 当前自动化只覆盖已列出的行为，不代表完成全面渗透测试或证明不存在漏洞。

## 升级注意

备份并保留 data 和自己的 .env；保持原访问地址、浏览器与端口。v1.2.0 → v1.2.1 不改变存档结构。首次从 v1.1.1 升级会要求重新输入入场密码，然后恢复有效旧凭证对应的座位。导入镜像并重新创建容器，仅重启旧容器不会升级。反向代理 YAML 为独立替代方案，不应与默认 YAML 叠加。

聊天草稿只暂存在当前标签页内，刷新或关闭后不会保留；消息不会自动重发。超时可能发生在服务器已收到消息之后，重发前请查看聊天记录，避免重复发送。
