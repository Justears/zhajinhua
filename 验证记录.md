# 后周炸炸炸 v1.2.0 验证记录

## 已执行

- 57 项测试通过：37 项规则引擎测试、8 项 HTTP 联机测试、7 项安全测试、5 项前端状态测试。其中包含 8 人 500 局随机压力测试，检查卡牌唯一、积分守恒和无死锁。
- 安全回归：修改密码撤销旧会话、新密码恢复旧座位、旧版签名升级、实时连接到期、按牌桌计算房主离线、HTTPS 来源校验、静态文件路径隔离及监听参数验证。
- 前端回归：旧响应不倒退状态、保持轮到自己的标题、离开再回同桌时丢弃旧错误、慢网合并轮询、旧大厅请求不改变新页面。
- 390 × 844 手机视口：登录、建房、七个独立 API 玩家入座、8 人发牌、其他玩家跟注同步、自己看牌、中文聊天、刷新恢复原座位。页面无横向溢出，浏览器无 error/warn 日志。
- 使用已有本地 Node 运行环境运行测试。因当前环境无法创建子进程，测试通过同进程执行器加载 node:test 用例并逐项等待，未声称执行了 npm test 命令；仓库仍可使用 npm test 运行相同测试文件。

## 离线包制作方式

保留 v1.1.1 中已核验的官方 Node AMD64 基础镜像层，替换应用层并更新镜像配置为 jinhua-friends:1.2.0。基础镜像版本未在本轮升级。制作脚本校验旧包 SHA256、TAR 头、全部文件层摘要和配置摘要，并逐字节比较新应用层与本版源码。ZIP 压缩数据逐文件解压比较；包内仅含空 data/.keep，不含实际密码、配置密钥或房间存档。

镜像 SHA256 见 SHA256SUMS.txt；ZIP SHA256 在 GitHub Release 附件处提供。

## 未执行

- 未在此电脑运行 Docker 引擎或执行 docker load / 容器实际启动；尚未在用户 DS923+ 上安装测试。
- 未验证用户的 DSM 反向代理、IPv6 入站规则或后端端口外网可达性，也未修改 NAS 配置。
- Compose 的本机绑定、cap_drop 和资源限制需在目标 Container Manager 上验证。绑定 127.0.0.1 不能替代对实际 Docker 版本和防火墙的核对，见 SECURITY.md。
- 当前自动化只覆盖已列出的行为，不代表完成全面渗透测试或证明不存在漏洞。

## 升级注意

备份并保留 data 和自己的 .env；保持原访问地址与浏览器。首次从 v1.1.1 升级会要求重新输入入场密码，然后恢复有效旧凭证对应的座位。修改密码后需要重新创建容器加载环境变量。反向代理 YAML 为独立替代方案，不应与默认 YAML 叠加。
