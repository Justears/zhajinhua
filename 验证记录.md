# 后周炸炸炸 v1.2.2 验证记录

审查日期：2026-09-21。基线为 GitHub main 的 v1.2.1 发布提交 abd681b01231bd19cb82e5dc9cf0eb00f423d3a5。

## 已执行

- Node.js v24.17.0 执行 83 项测试全部通过：43 项规则引擎测试、8 项 HTTP 联机测试、11 项安全测试、21 项前端状态测试。包含 8 人 500 局随机压力测试，检查卡牌唯一、积分守恒和无死锁。
- 全押回应回归：先跟注的玩家在别人全押加注后仍可回应；三人局中间玩家弃牌后，最后一名玩家也保留回应权；看牌不算完成回应。
- 浏览器实际操作双人局，确认全押后保持下注阶段，另一人可以弃牌并正常结算；检查全押金额、二次确认、结算和八人座位展示。
- 实际桌面浏览器在 320×740、390×844 和 1440×1000 视口检查大厅及牌桌；窄屏无横向溢出，检查长昵称、弹窗按钮、加入入口与标题布局。
- 浏览器实际验证密码显示/隐藏切换及登录提交；键盘焦点、登录到期、全押确认和日志滚动保持另有模拟 DOM 回归。
- 超时存档失败测试模拟一次磁盘写入错误，验证迟到行动返回 503 且存档不变，后续超时重试能够完成结算。
- 拒绝非标准卡牌编码与继承属性动作名；验证合法全押加注金额与闷牌换算。
- 本地环境无法用标准测试运行器创建子进程（EPERM），使用同进程模式 node --test --test-isolation=none test/*.test.cjs；CI 使用正常测试运行器。

## 正式发布验证

GitHub Actions 从固定基础镜像构建 Docker 映像，在映像内执行完整测试，完成只读文件系统、移除 capabilities、限制内存与进程数条件下的启动、健康检查和登录验证后打包。上传后核对大小和 SHA256，再公开 Release。每个安装包中的 发布验证.md 记录发布提交与 Actions 链接，以对应运行结果为准。

包内仅包含空 data/.keep，不包含测试牌桌、实际密码、配置密钥或用户存档。使用本 Release 的 SHA256SUMS.txt 校验 ZIP，镜像校验值在 ZIP 内。

## 验证边界

- 浏览器尺寸模拟不等于真实 Android/iPhone 触摸测试；未在用户 DS923+ 上安装。
- 未连接或修改用户的 NAS、DSM 反向代理、防火墙或 IPv6 配置，未验证实际外网入口。
- 本地没有 Docker 引擎；容器运行检查由 Linux CI 完成，不能代替目标 Container Manager 实机检查。
- 未进行全面渗透测试或依赖漏洞扫描，自动化覆盖不代表证明不存在漏洞。

## 升级注意

升级前先结束正在进行的牌局，再停止项目，备份并保留整个 data 和自己的 .env。导入 jinhua-friends:1.2.2 并重新创建容器，保持原端口和访问地址。v1.2.1 存档可直接读取，无需手工迁移；新增回应状态会在下一局初始化。已经结算的旧局不会重新计算。

首次从 v1.1.1 升级会要求重新输入入场密码，然后恢复有效旧凭证对应的座位。反向代理 YAML 是独立替代方案，不应与默认 YAML 叠加。恢复旧版时使用升级前的 data 备份。
