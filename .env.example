# Copy to .env and choose your own password (8+ characters).
ROOM_PASSWORD=

# NAS-side port. Keep the existing port when upgrading; 18780 is the default.
GAME_PORT=18780

# Required only by docker-compose.proxy.yml. Example, replace with your URL:
# PUBLIC_ORIGIN=https://game.example.com:5588
PUBLIC_ORIGIN=
