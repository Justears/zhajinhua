# 后周炸炸炸：群晖安全部署

本项目只提供好友娱乐积分牌局。限制入口可以减少直接访问路径，HTTPS 可以保护传输；它们不能保证游戏或 NAS 没有漏洞。

## 先选一种部署方式

| 方式 | 配置文件 | 访问方法 |
| --- | --- | --- |
| 家里先测试 | docker-compose.yml | http://群晖内网IP:18780 |
| 使用 DSM 的 HTTPS 反向代理 | docker-compose.proxy.yml | 你配置的 HTTPS 游戏网址 |
| 下载官方 Node 镜像后直接运行源码 | docker-compose.online.yml | 默认同局域网测试模式 |

三个配置是独立替代方案，不应叠加使用，不应同时运行。正式使用哪个，就在 Container Manager 项目中选择对应的 YAML。

## 反向代理方式

1. 停止旧项目，备份 data 和自己的 .env。保留原数据，导入新版离线镜像。
2. 编辑 .env：填写 ROOM_PASSWORD；GAME_PORT 填群晖后端端口（默认 18780）；PUBLIC_ORIGIN 填浏览器真正使用的完整 HTTPS 入口，只含协议、域名、端口，不要填路径。不要公开 .env。
3. 选择 docker-compose.proxy.yml 创建项目。其映射为 `127.0.0.1:${GAME_PORT}:8080`，容器内仍监听 `0.0.0.0:8080`。不要把容器内部 BIND_ADDRESS 改成 127.0.0.1，否则端口映射会连接不到程序。
4. DSM：控制面板 → 登录门户 → 高级 → 反向代理。来源使用你的 HTTPS 域名和外部端口；目的地为 HTTP、127.0.0.1、GAME_PORT 对应的端口。为来源配置匹配的有效证书，并保留外部 Host 请求头（包括自定义端口）。
5. 游戏使用 SSE 实时连接，不是 WebSocket。代理需要允许长连接并关闭响应缓冲；服务端也会返回 `X-Accel-Buffering: no`。默认页面有轮询作为恢复手段。
6. 浏览器统一通过 HTTPS 入口访问。此配置启用了 Secure Cookie，直接使用 HTTP 登录不能正常保持会话。

举例：希望保留“外面 5588、后面 8899”的习惯，可以把 GAME_PORT 设为 8899，PUBLIC_ORIGIN 设为 `https://game.example.com:5588`；反向代理来源用对应 HTTPS 域名和 5588，目的地为 `http://127.0.0.1:8899`。域名只是示例，需自行替换。Docker 版会映射到容器 8080；不必另外安装 SPK。

推荐游戏使用独立子域名。同一域名的不同端口并不能隔离 Cookie，不应把互不信任的应用仅靠端口区分。PUBLIC_ORIGIN 会限制 API 的 Host 及浏览器写请求的完整 Origin；它不是身份验证的替代品。程序不会盲目信任 X-Forwarded-Host 或 X-Forwarded-For。

## 如何判断后端端口有没有向外网开放

1. 手机关闭 Wi-Fi，用具备 IPv6 的移动网络先确认 HTTPS 游戏入口能打开。
2. 再访问同域名的 HTTP 后端端口，例如 `http://你的域名:8899`。不要在这个测试入口输入密码。如果直接返回游戏或服务响应，说明这条直连路径存在；注意检查浏览器有没有自动切换 HTTPS 或跳到其他端口。
3. 打不开只说明这次测试未成功，不能单凭一次失败证明所有网络路径都已关闭。检查 Container Manager 的实际端口绑定、路由器 IPv6 入站规则和 DSM 防火墙，并检查是否还有其他代理转发到后端。
4. 对外只允许需要的 HTTPS 入口，取消路由器对后端端口的入站放行；IPv4 若存在转发，也一并核对。没有公网 IPv4 不代表 IPv6 没有入口。

正常 bridge 网络下，将宿主端口绑定为 127.0.0.1 可限制直接访问范围。但 Docker 官方说明，28.0.0 之前版本存在同一二层网络内访问 localhost 发布端口的问题；群晖实际引擎版本、路由及防火墙仍需核对，不能以配置文本替代实机验证。

参考：[Docker 官方端口发布说明](https://docs.docker.com/engine/network/port-publishing/)。

## 保护 NAS 数据

- 仅为游戏挂载专用 data 目录；不挂载 /volume1、家庭目录、其他共享目录、Docker 套接字。
- 保留只读应用文件系统、cap_drop: ALL、no-new-privileges、内存/CPU/进程数限制，不使用 privileged 或 host 网络。
- 当前为兼容旧 data 权限，容器仍默认使用容器内 root；移除 capabilities 不等于非 root 运行。需要非 root 时，应先备份并针对游戏目录设置所选 UID/GID 权限，再验证启动，不要盲目修改整个存储卷权限。
- 上述限制不能阻止所有通过内网服务扩大影响的攻击，也不能保证容器逃逸不发生。保持 DSM、Container Manager 和应用更新，保留独立备份。
- PUBLIC_ORIGIN 和 HTTPS 不是防火墙；反向代理仍会把合法入口收到的请求交给游戏。

## 密码和登录升级

更改 ROOM_PASSWORD 后要重新创建容器使配置生效。新版本会撤销旧登录；同一浏览器携带尚未过期的旧签名，再输入正确的新密码后，可以找回原座位。第一次从 v1.1.1 升级也会要求重新验证。不要清除 Cookie 或删除 config.json，否则可能失去座位身份。

会话最长 90 天，到期后需要重新登录；到期的实时连接也会结束。房主在线状态按实际访问该牌桌计算，手机长期切后台后可能被视为暂离；两分钟无活动后，其他在座好友可接任。

## 验证边界

本版本有规则、HTTP 接口及浏览器检查，但没有在你的 DS923+ 上实际启动 Docker，也没有修改你的防火墙或反向代理。只能证明已经执行的检查结果，不能承诺“被破解也只影响 data”。
